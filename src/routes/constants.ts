export const CANVAS_SIZE = 500;
export const CELL_SIZE = 5;
export const CELL_COUNT = (CANVAS_SIZE * CANVAS_SIZE) / (CELL_SIZE * CELL_SIZE);
export const CELLS_PER_ROW = CANVAS_SIZE / CELL_SIZE;

export const FPS = 60;
export const FRAME_DURATION = 1000 / FPS;
